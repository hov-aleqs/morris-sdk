<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Рекламный идентификатор: партнёр обязан объявить его сбор в форме
         данных стора, готовая формулировка — в документации SDK. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <application>
        <!-- configChanges обязателен. Без него поворот экрана или появление
             клавиатуры пересоздают Activity: воспроизведение обрывается, а
             трекинг либо шлёт start второй раз, либо теряет complete. -->
        <activity
            android:name="com.morris.ads.MorrisAdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize|navigation"
            android:exported="false"
            android:hardwareAccelerated="true"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen" />
    </application>
</manifest>
